# Insplit Web Admin Dashboard Environment Configuration
# API URL (Leave empty in production if served from same origin, or set to your backend API URL)
VITE_API_URL=http://localhost:5000/api

# Optional HTTPS direct APK link. By default the showcase discovers the latest GitHub APK.
VITE_ANDROID_DOWNLOAD_URL=
